plugins {
	id 'java'
}

sourceSets {
	main {
		java {
			srcDir 'src'
		}
		resources {
			srcDir 'conf'
		}
	}
}

dependencies {
	compile group: 'com.google.code.gson', name: 'gson', version: '2.8.4'
}
